package tp1_C;

public interface Salaried {
	public long totalSalary(); // Le reste des méthodes on ne les utilise pas depuis Payment. 
	//Faire attention aux méthodes qui servent a rien qu'on ajoute aux interface
}
